{
  "name": "reset-password",
  "version": "1.0.0",
  "description": "Verifies OTP and resets user password via admin SDK",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "format": "prettier --write ."
  },
  "dependencies": {
    "node-appwrite": "^20.2.1"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
